// $Id$

Drupal.behaviors.listmixer = function() {
  // Based on Drupal.settings.listmixer.interactionname
  // foreach interaction preset, load the target id - look for it.
  // if it is found, load whatever javascripts are also supposed to be loaded (includes)
  // Drupal.settings.listmixer.interactionname.behaviors.script = path   path to module /behaviors/push/content/node/behaviorname
  // @TODO: Could have problems with relative paths
  
  /* Loading javascript includes
  http://docs.jquery.com/Ajax/jQuery.getScript
  
  $.getScript( Drupal.settings.listmixer.interactionname.behaviors.script + ".inc.js", function(){
  });
  
  // without callback
  $.getScript( Drupal.settings.listmixer.interactionname.behaviors.script + ".inc.js");
  */
}


Drupal.behaviors.listmixer.activate = function() {

}

Drupal.behaviors.listmixer.assign = function() {

}

Drupal.behaviors.listmixer.interact = function() {

}

Drupal.behaviors.listmixer.push = function() {

}

Drupal.behaviors.listmixer.submit = function() {

}